
const MongoFunction = async() => {

  const query = "Titanic"
  //const response = await fetch(`/api/mongodb_new/route?query=${encodeURIComponent(query)}`)

  return(
    <>
      hello world!!
      <br/>
      This is a whole new worldwhole new worldwhole new worldwhole new worldwhole new worldwhole new world
    </>
  );
}

export default MongoFunction;